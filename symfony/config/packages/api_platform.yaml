api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
        api_keys:
            Подпишите ваши запросы JWT токеном и добавьте перед ним префикс "Bearer ":
                name: Authorization
                type: header